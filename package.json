{
  "name": "health-app-backend",
  "version": "1.0.0",
  "description": "Backend API for Health App using JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 5000",
    "dev": "json-server --watch db.json --port 5000"
  },
  "keywords": ["json-server", "api", "backend"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
